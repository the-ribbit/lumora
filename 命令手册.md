# 澄境 Lumora · 完整命令手册（Windows）

> 所有事实均来自项目实际文件：`package.json`、`vite.config.ts`、`.git/config`、目录结构。
>
| 项目实际信息 | 值 |
|---|---|
| 项目目录 | `D:\projects\test\炉子_测试\lumora` |
| 实测环境 | Node `v24.18.0`、npm `11.16.0`、git `2.34.0.windows.1` |
| 构建工具 | Vite `^8.3.0` + `@vitejs/plugin-react ^6.1.1` |
| 框架/语言 | React `^19.2.8`、TypeScript `~6.0.2` |
| 样式/检查 | Tailwind CSS `^3.4.19`（非 v4）、postcss、autoprefixer、oxlint |
| 图标库 | lucide-react `^1.46.0` |
| 远程仓库 | `https://github.com/the-ribbit/lumora.git` |
| 部署方式 | `gh-pages` 分支托管 `dist/`（未使用 gh-pages 插件） |
| 自定义域名 | `enterlumora.top`（根路径部署，`public/CNAME` 构建时自动进入 `dist/`） |
| 线上地址 | `https://enterlumora.top/`（旧地址 `https://the-ribbit.github.io/lumora/` 会自动 301 跳转） |
| Vite base | `'/'`（根路径；曾用 `'/lumora/'`，启用自定义域名后已改） |
| 本仓库 git 代理 | `http://127.0.0.1:7890`（Clash 本地端口） |

**CMD 与 PowerShell 的主要差异**（下文只在有差异处分别给出）：

| 操作 | PowerShell | CMD |
|---|---|---|
| 设临时环境变量 | `$env:NAME="value"` | `set NAME=value` |
| 强制删目录 | `Remove-Item -Recurse -Force dist` | `rmdir /s /q dist` |
| 删除单个文件 | `Remove-Item file` | `del file` |
| 路径含中文/空格 | 用双引号包起来（两种终端都要） | 同左 |

---

## 1. 环境准备（Node / npm / cache / prefix）

### 1.1 检查 Node 与 npm

- **用途**：确认工具链可用
- **目录**：任意
- **频次**：一次性
- **成功标志**：能打印版本号；本项目实测为 Node `v24.18.0`、npm `11.16.0`

PowerShell / CMD 通用：

```powershell
node -v
npm -v
git --version
```

> 注意：Vite 8 需要较新的 Node（建议 Node 20.19+ / 22.12+ LTS，本机 Node 24 实测正常）。

### 1.2 查看 npm 缓存与全局安装目录

- **用途**：确认 cache/prefix 位置，避免踩到 `C:\Program Files` 权限问题
- **目录**：任意
- **频次**：一次性

```powershell
npm config get cache
npm config get prefix
```

> 本机实测缓存目录位于 `D:\Program Files\nodejs\node_cache`。

### 1.3（可选）把缓存改到有权限的目录

- **用途**：当出现缓存目录只读 / EPERM / `not allow operate files` 时使用
- **目录**：任意
- **频次**：一次性（不需要可跳过）

PowerShell：

```powershell
npm config set cache "D:\npm-cache" --global
npm config set prefix "D:\npm-global" --global
```

CMD：

```cmd
npm config set cache "D:\npm-cache" --global
npm config set prefix "D:\npm-global" --global
```

- **成功标志**：再次执行 `npm config get cache` 显示新路径
- **注意**：本项目根目录**没有** `.npmrc`，以上是用户级配置，不进仓库

---

## 2. 创建项目（仅从零搭建时需要；已有项目跳过本节）

> 本项目目录已存在且包含素材，**克隆/接手现有项目时不需要执行本节**，直接从第 3 节 `npm install` 开始。

### 2.1 用 Vite 模板生成 React+TS 工程

- **用途**：从零创建同名项目
- **目录**：你希望项目所在的**父目录**（例如 `D:\projects\test\炉子_测试`）
- **频次**：一次性

PowerShell：

```powershell
cd "D:\projects\test\炉子_测试"
npm create vite@latest lumora -- --template react-ts
cd lumora
```

CMD：

```cmd
cd /d "D:\projects\test\炉子_测试"
npm create vite@latest lumora -- --template react-ts
cd lumora
```

- **成功标志**：生成 `package.json`、`src/`、`index.html`、`tsconfig*.json` 等

### 2.2 放入本地素材

- **用途**：网站依赖 `input_assets/` 中的 4 个 mp4、1 个 png、2 个 ttf（共 7 个文件），由 Vite 打包
- **目录**：项目根
- **频次**：一次性

```powershell
# 确认素材齐全（应列出 7 个文件）
Get-ChildItem input_assets
```

CMD：

```cmd
dir input_assets
```

必须包含：`lumora-scene-01-golden-hour.mp4`、`lumora-scene-02-still-water.mp4`、`lumora-scene-03-deep-woods.mp4`、`lumora-scene-04-quiet-dawn.mp4`、`lumora-foreground-overlay.png`、`lumora-display-regular.ttf`、`lumora-display-italic.ttf`。

---

## 3. 安装依赖

### 3.1 接手/恢复本项目（推荐，有 package-lock.json）

- **用途**：按锁文件安装全部依赖
- **目录**：`D:\projects\test\炉子_测试\lumora`
- **频次**：首次或依赖变更后

```powershell
cd "D:\projects\test\炉子_测试\lumora"
npm install
```

- **成功标志**：生成 `node_modules/`，结尾无 `npm error`

### 3.2 从零搭建时装 Tailwind（本项目为 Tailwind v3，勿装 v4）

- **用途**：新建项目时补齐样式链路
- **目录**：项目根
- **频次**：一次性

```powershell
npm install -D tailwindcss@^3.4.19 postcss@^8.5.28 autoprefixer@^10.6.1
npx tailwindcss init -p
```

- **成功标志**：生成 `tailwind.config.js`、`postcss.config.js`
- **注意**：必须锁 v3 大版本；Tailwind v4 配置方式完全不同。本仓库的两个配置文件已存在，接手时**不要**再执行 init

### 3.3 安装图标库

- **用途**：项目用到 lucide 图标（如成功态的对勾）
- **目录**：项目根
- **频次**：一次性

```powershell
npm install lucide-react@^1.46.0
```

### 3.4 代码检查（可选）

- **用途**：运行 oxlint
- **目录**：项目根
- **频次**：按需

```powershell
npm run lint
```

---

## 4. 本地开发运行

### 4.1 启动开发服务器

- **用途**：热更新开发
- **目录**：项目根
- **频次**：每次开发

```powershell
npm run dev
```

- **成功标志**：输出 `VITE v8.x ready` 和 `Local: http://localhost:5173/`
- **注意**：按 `Ctrl + C` 停止；仅适配 1280×800 及以上桌面浏览器，无移动端布局

---

## 5. 构建与本地预览

### 5.1 正式构建

- **用途**：类型检查（`tsc -b`）+ 产出 `dist/`
- **目录**：项目根
- **频次**：每次部署前

```powershell
npm run build
```

- **成功标志**：`✓ built in x.xs`，退出码 0，`dist/` 下出现 `index.html` 与 `assets/`（含 4 个 mp4、1 个 png、2 个 ttf）

### 5.2 确认 base 路径已生效

- **用途**：防止线上资源 404 白屏
- **目录**：项目根
- **频次**：每次构建后建议检查

```powershell
Get-Content dist/index.html
```

CMD：

```cmd
type dist\index.html
```

- **成功标志**：脚本与样式地址以 `/assets/` 开头（根路径），例如 `src="/assets/index-xxxx.js"`；同时 `dist/CNAME` 文件存在，内容为 `enterlumora.top`。前缀来自 `vite.config.ts` 的 `base: '/'`
- **注意**：**不要**把 `base` 写进 `build` 字段内（Vite 中 `base` 是顶层配置）

### 5.3 本地预览构建产物

- **用途**：在与线上最接近的环境下验证 dist
- **目录**：项目根
- **频次**：按需

```powershell
npm run preview
```

- **成功标志**：输出 `http://localhost:4173/`
- **注意**：`base: '/'` 时直接访问 `http://localhost:4173/` 即可完整加载资源；CNAME 不影响本地预览

---

## 6. Git 初始化与提交

### 6.1 初始化仓库（仅首次）

- **用途**：本项目最初不是 git 仓库时执行
- **目录**：项目根
- **频次**：一次性

PowerShell：

```powershell
cd "D:\projects\test\炉子_测试\lumora"
git init -b main
git add -A
git commit -m "feat: 澄境正念应用首屏与四个正文区"
```

CMD：

```cmd
cd /d "D:\projects\test\炉子_测试\lumora"
git init -b main
git add -A
git commit -m "feat: 澄境正念应用首屏与四个正文区"
```

- **成功标志**：输出 `[main (root-commit) ...]`
- **注意**：`.gitignore` 已忽略 `node_modules`、`dist`、`*.local`、`.vscode/*`；`LF will be replaced by CRLF` 警告无害

### 6.2 日常提交

- **用途**：保存代码改动
- **目录**：项目根
- **频次**：每次改动

```powershell
git status
git add -A
git commit -m "feat: 改动说明"
```

### 6.3（国内网络）配置仓库代理

- **用途**：解决连不上 github.com 443 超时。本机 Clash HTTP 端口为 **7890**
- **目录**：项目根
- **频次**：一次性，仅本仓库生效（与本项目当前 `.git/config` 实际配置一致）

```powershell
git config http.proxy http://127.0.0.1:7890
git config https.proxy http://127.0.0.1:7890
git config http.postBuffer 524288000
git config http.version HTTP/1.1
```

- **成功标志**：`git config --get http.proxy` 输出代理地址
- **注意**：
  - 本仓库视频约 80MB，`postBuffer` 调大可降低大推送失败概率
  - 若以后不用代理，移除配置：

```powershell
git config --unset http.proxy
git config --unset https.proxy
```

### 6.4 关联远程并推送 main

- **用途**：把源码推到 GitHub
- **目录**：项目根
- **频次**：关联一次；推送每次更新

```powershell
git remote add origin https://github.com/the-ribbit/lumora.git
git push -u origin main
```

- **成功标志**：出现 `* [new branch] main -> main`；已关联过则后续只需 `git push`
- **注意**：
  - 远程地址必须纯净，不要带反引号或多余空格
  - 首次推送会弹出 Git Credential Manager 登录窗口，请在弹窗里完成 GitHub 授权（凭据助手实测为 `manager-core`）
  - 非交互终端若卡住，可设 `$env:GIT_TERMINAL_PROMPT=1`（PowerShell）后重试

---

## 7. GitHub Pages 部署

> 本项目**没有安装 gh-pages npm 包**，实际采用「在 dist 内建临时 git 仓库 → 强推 gh-pages 分支」的方式。

### 7.1 确认 vite base 与 CNAME（部署前置条件）

- **用途**：站点已绑定自定义域名根路径，base 必须为根路径，否则整页白屏/黑屏
- `vite.config.ts` 中必须是：

```ts
export default defineConfig({
  plugins: [react()],
  base: '/',
})
```

- 仓库 `public/CNAME` 必须存在且内容为 `enterlumora.top`，构建后会出现在 `dist/CNAME`。这样每次 force push 覆盖 gh-pages 时都不会丢失自定义域名绑定（无需手动往 dist 里补 CNAME）

### 7.2 构建

- **目录**：项目根
- **频次**：每次部署

```powershell
npm run build
```

### 7.3 把 dist 推到 gh-pages 分支

- **用途**：更新 Pages 内容
- **目录**：先在项目根构建，再**进入 dist**
- **频次**：每次部署

PowerShell：

```powershell
cd "D:\projects\test\炉子_测试\lumora\dist"
git init -b gh-pages
git config http.proxy http://127.0.0.1:7890
git config https.proxy http://127.0.0.1:7890
git add -A
git commit -m "deploy: 澄境 GitHub Pages"
git remote add origin https://github.com/the-ribbit/lumora.git
git push -f origin gh-pages
```

CMD：

```cmd
cd /d "D:\projects\test\炉子_测试\lumora\dist"
git init -b gh-pages
git config http.proxy http://127.0.0.1:7890
git config https.proxy http://127.0.0.1:7890
git add -A
git commit -m "deploy: 澄境 GitHub Pages"
git remote add origin https://github.com/the-ribbit/lumora.git
git push -f origin gh-pages
```

- **成功标志**：`* [new branch] gh-pages -> gh-pages`（首次）或强制更新成功
- **注意**：
  - `-f` 是有意的：每次部署都用全新历史覆盖 gh-pages
  - 80MB 视频上传需要数分钟，期间无进度输出属正常，不要中断
  - **推完务必删掉 dist 里的临时仓库**（见 7.4）

### 7.4 清理 dist 内的临时 .git

- **用途**：避免与主仓库冲突
- **目录**：项目根
- **频次**：每次部署后

PowerShell：

```powershell
cd "D:\projects\test\炉子_测试\lumora"
Remove-Item -Recurse -Force dist\.git
```

CMD：

```cmd
cd /d "D:\projects\test\炉子_测试\lumora"
rmdir /s /q dist\.git
```

### 7.5 GitHub 网页设置（仅首次需要）

- 打开 `https://github.com/the-ribbit/lumora/settings/pages`
- **Build and deployment → Source** 选 `Deploy from a branch`
- **Branch** 选 `gh-pages`，目录选 `/ (root)`，保存
- 等待 1–2 分钟，页面顶部出现绿色提示与链接

---

## 8. 部署后验证（远程资源检查）

### 8.1 打开线上页面

- **地址**：`https://enterlumora.top/`
- **成功标志**：首屏金色时刻视频自动播放、白色「澄境」导航、四个风景标签、正文四区可滚动到达
- 旧地址 `https://the-ribbit.github.io/lumora/` 应自动 301 跳转到自定义域名

### 8.2 验证视频资源可访问

- **用途**：确认 4 个大文件已正确发布
- **目录**：任意
- **频次**：每次部署后

PowerShell（文件名 hash 会随构建变化，先看 `dist/assets/` 里的实际名字）：

```powershell
Invoke-WebRequest -Uri "https://enterlumora.top/assets/lumora-scene-01-golden-hour-VF1AzCAN.mp4" -Method Head -UseBasicParsing | Select-Object StatusCode
```

CMD：

```cmd
curl -I https://enterlumora.top/assets/lumora-scene-01-golden-hour-VF1AzCAN.mp4
```

- **成功标志**：`StatusCode: 200`（或 curl 显示 `HTTP/2 200`）
- **注意**：hash 文件名每次构建可能不同，以 `dist/index.html` / `dist/assets/` 实际产物为准

### 8.3 确认无外部资源

- 浏览器 F12 → Network，勾选 Media/Font/Img，确认请求域名**只有** `the-ribbit.github.io`，无任何 CDN、在线字体或远程视频
- 控制台无红色报错（媒体偶发的 `net::ERR_ABORTED` 是浏览器 range 请求重发的正常行为，视频 `readyState=4` 即正常）

---

## 9. 常见错误修复

| 现象 | 原因 | 处理 |
|---|---|---|
| `Failed to connect to github.com port 443 ... Timed out` / `Connection was reset, errno 10054` | 国内网络直连 GitHub 不稳定 | 执行 6.3 配置本地代理（确认 Clash 在 7890 端口），再推送 |
| `cannot lock ref 'refs/heads/main': reference already exists` | 远程已有同名分支；很多时候**数据其实已推上去** | 先 `git fetch origin` 再 `git log origin/main --oneline`，若含你的提交则无需处理；否则 `git pull --rebase origin main` 后再 push |
| 推送长时间无输出、多个 git 进程 0 CPU | 卡在凭据交互（非交互终端） | `Stop-Process -Name git -Force` 结束后，PowerShell 设 `$env:GIT_TERMINAL_PROMPT=1` 重推，留意桌面弹出的 Credential Manager 登录窗 |
| 自定义域名根路径下黑屏/白屏，资源全 404 | 仍在用子路径 `base: '/lumora/'` | 确认 `base: '/'`（顶层配置）、`dist/CNAME` 存在，重新 `npm run build` 并重新部署 |
| 部署后 GitHub Pages 的 Custom domain 变空 | force push 覆盖 gh-pages 时丢了 CNAME | 在 `public/CNAME` 放域名（本项目已配置），让构建自动携带 |
| 大文件推送中断 | HTTP 缓冲/版本问题 | `git config http.postBuffer 524288000` + `git config http.version HTTP/1.1`（本项目已配） |
| `npm run build` 失败在 `tsc -b` | TypeScript 类型错误 | 先修类型错误（如未使用变量，项目开启了 `noUnusedLocals`），再构建 |
| 控制台视频 `net::ERR_ABORTED` | Chromium 对自动播放媒体的初始 range 请求取消重发 | 正常现象，视频能播放即可，不用改 |
| `warning: LF will be replaced by CRLF` | Windows 换行符提示 | 无害，无需处理 |
| npm 报 EPERM / `Program Files` 权限 | 缓存/全局目录无写权限 | 见 1.3，把 cache/prefix 改到 `D:\` 下 |
| Trae 沙箱提示 `Not allow operate files ...` 导致退出码非 0 | 工具沙箱拦截全局路径写入 | 在工具中选择「沙箱外运行」；构建本身通常已成功，看输出里的 `✓ built` |

---

## 附 A：从零到部署的最短顺序清单

```powershell
# 0) 一次性：建项目（已有项目从第 1 步开始）
cd "D:\projects\test\炉子_测试"
npm create vite@latest lumora -- --template react-ts
cd lumora
npm install
npm install -D tailwindcss@^3.4.19 postcss@^8.5.28 autoprefixer@^10.6.1
npx tailwindcss init -p
npm install lucide-react@^1.46.0
#   放入 input_assets 7 个素材；配置好 tailwind.config.js / postcss.config.js / index.css
#   vite.config.ts 设 base: '/'；public/CNAME 内容填自定义域名

# 1) 本地验证
npm run dev        # http://localhost:5173

# 2) 构建
npm run build

# 3) 源码上 main
git init -b main
git config http.proxy http://127.0.0.1:7890
git config https.proxy http://127.0.0.1:7890
git add -A
git commit -m "feat: 澄境正念应用首屏与四个正文区"
git remote add origin https://github.com/the-ribbit/lumora.git
git push -u origin main

# 4) dist 上 gh-pages
cd dist
git init -b gh-pages
git config http.proxy http://127.0.0.1:7890
git config https.proxy http://127.0.0.1:7890
git add -A
git commit -m "deploy: 澄境 GitHub Pages"
git remote add origin https://github.com/the-ribbit/lumora.git
git push -f origin gh-pages
cd ..
Remove-Item -Recurse -Force dist\.git

# 5) 首次还需在 GitHub Settings → Pages 选择 gh-pages 分支并填写 Custom domain（之后 CNAME 已随仓库管理）
# 6) 访问 https://enterlumora.top/
```

## 附 B：日常更新重新部署的最短命令

```powershell
cd "D:\projects\test\炉子_测试\lumora"

# 1) 提交并推送源码
git add -A
git commit -m "feat: 更新说明"
git push

# 2) 构建并发布 Pages
npm run build
cd dist
git init -b gh-pages
git config http.proxy http://127.0.0.1:7890
git config https.proxy http://127.0.0.1:7890
git add -A
git commit -m "deploy: update"
git remote add origin https://github.com/the-ribbit/lumora.git
git push -f origin gh-pages
cd ..
Remove-Item -Recurse -Force dist\.git
```
